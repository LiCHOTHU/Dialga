All architectural details of the encoder, the spatial dynamics code, the decoder,
and the camera-pose path are given in \cref{sec:method} and \cref{eq:global-dyn}--%
\cref{eq:objective}; hyperparameters, the exact float budgets, and the training
recipe are in \cref{sec:setup} and the appendix. \method{} operates on frozen
Wan-2.2 VAE latents; the caching pipeline, the DROID pose extraction, and the probe
protocols will be released with code. Every reported number states its dataset,
split, baseline, and float budget, and prior-model numbers are marked \emph{[P]}.
